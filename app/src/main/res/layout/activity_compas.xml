<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/activity_compass"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="20dp"
    android:paddingLeft="20dp"
    android:paddingRight="20dp"
    android:paddingTop="20dp"
    tools:context="pl.lodz.p.imsi.maciekka.uprawnienia.CompasActivity">

    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_centerHorizontal="true"
        android:textAlignment="center"
        android:textSize="46sp"
        android:textColor="#000"
        android:textStyle="bold"
        android:text="@string/stopnie"
        android:id="@+id/txt_azimuth" />

    <ImageView
        android:paddingTop="40dp"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:src="@drawable/compass"
        android:layout_below="@id/txt_azimuth"
        android:id="@+id/img_compass"/>

    <ImageView
        android:paddingTop="50dp"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:src="@drawable/compassarrow"
        android:layout_below="@id/txt_azimuth"
        android:id="@+id/img_compassarrow"/>

</RelativeLayout>
